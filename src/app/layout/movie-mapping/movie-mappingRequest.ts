import { Header } from '../../shared/services/header';

export class MovieMapping {

    movieId: number
    header: Header;
    constructor(

        movieId: number) {

        movieId = movieId;
    }
}
